package com.example.caseprocessor;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Spring Boot application for processing Excel files and creating cases
 */
@SpringBootApplication
public class CaseProcessorApplication {

    public static void main(String[] args) {
        SpringApplication.run(CaseProcessorApplication.class, args);
    }
}
